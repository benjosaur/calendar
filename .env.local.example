# Set by `bunx convex dev`
NEXT_PUBLIC_CONVEX_URL=

# Convex deployment env vars are set in the Convex dashboard or via the CLI, NOT here:
#   bunx convex env set ANTHROPIC_API_KEY sk-ant-...
# `bunx @convex-dev/auth` will set JWT_PRIVATE_KEY / JWKS / SITE_URL on the deployment.
